# Database (PostgreSQL)
DATABASE_URL="postgresql://mimi:mimi_secret@localhost:5433/mimi_db"

# Admin panel: pass in URL ?token=... or header x-admin-token
ADMIN_TOKEN="your-secret-admin-token"

# Telegram Bot (create via @BotFather)
TG_BOT_TOKEN="123456:ABC-DEF..."

# Chat/group where new order notifications are sent
# How to get: add bot to group, send a message, then open:
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# Find "chat":{"id": -123456789} — use that number (with minus for groups)
TG_CHAT_ID="-123456789"

# Public URL of the Mini App (for production)
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# Оплата ЮKassa (если не заданы — заказы создаются без оплаты)
# YOOKASSA_SHOP_ID="123456"
# YOOKASSA_SECRET_KEY="live_..."
